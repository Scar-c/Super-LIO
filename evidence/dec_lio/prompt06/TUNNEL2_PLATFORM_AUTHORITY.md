# Tunnel2 platform authority

The primary sequence is `Tunneling_tunnel2_alpha`: GEODE Alpha, VLP-16 and
UGV, as specified by the GEODE dataset authority and Prompt06 scope.  This is
an authority label; it is not inferred from the estimated trajectory,
velocity, or the weak-axis result.

The input bag is:

`/home/lc/dec_lio/bag/GEODE/Tunneling_tunnel2_alpha.bag`

The paired reference is:

`/home/lc/dec_lio/bag/GEODE/Tunneling_tunnel2.txt`

The public dataset authority used for the sequence/platform naming is the
[GEODE localization evaluation](https://github.com/PengYu-Team/GEODE_dataset#localization-evaluation).
The Alpha bag was run with the canonical Dec-LIO Alpha configuration; no
platform label was derived from motion.
