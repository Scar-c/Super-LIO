diff --git a/scripts/super_livo/experiments/run_offline_variant.sh b/scripts/super_livo/experiments/run_offline_variant.sh
index 1e35954..c75f8e7 100755
--- a/scripts/super_livo/experiments/run_offline_variant.sh
+++ b/scripts/super_livo/experiments/run_offline_variant.sh
@@ -17,6 +17,7 @@ CAM_TOPIC="${5:-}"
 CAM_CALIB="${6:-}"
 DURATION="${7:--1}"
 S0_AUDIT="${8:-0}"
+CAM_OFFSET="${9:-0.0}"
 
 ROOT=/home/lc/super_livo
 NODE="$ROOT/devel/.private/super_lio/lib/super_lio/super_lio_offline_node"
@@ -125,6 +126,7 @@ fi
 if [ -n "$CAM_CALIB" ]; then
   rosparam set /camera/calib_file "$CAM_CALIB"
 fi
+rosparam set /camera/time_offset "$CAM_OFFSET"
 
 # ---- param readback (fail before node start if any key param missing) ----
 read_param() {
@@ -133,6 +135,7 @@ read_param() {
 is_true() { [ "$1" = "True" ] || [ "$1" = "true" ]; }
 is_false() { [ "$1" = "False" ] || [ "$1" = "false" ]; }
 cam_en=$(read_param /camera/enabled)
+cam_off=$(read_param /camera/time_offset)
 cam_ep=$(read_param /lio/camera_epoch/enabled)
 app=$(read_param /lio/v4/apply)
 gate=$(read_param /lio/v4/outlier_gate)
@@ -142,7 +145,7 @@ skip_fd=$(read_param /lio/v2/skip_fd)
 hb0=$(read_param /lio/hb0/enabled)
 vp=$(read_param /lio/vp/enabled)
 s0_aud=$(read_param /lio/s0/audit)
-echo "=== variant=$VARIANT readback: camera=$cam_en camera_epoch=$cam_ep apply=$app gate=$gate v0=$v0 v2=$v2 skip_fd=$skip_fd hb0=$hb0 vp=$vp s0_audit=$s0_aud ==="
+echo "=== variant=$VARIANT readback: camera=$cam_en camera_epoch=$cam_ep apply=$app gate=$gate v0=$v0 v2=$v2 skip_fd=$skip_fd hb0=$hb0 vp=$vp s0_audit=$s0_aud cam_offset=$cam_off ==="
 case "$VARIANT" in
   b0) is_false "$cam_en" || { echo "FAIL readback camera"; exit 4; }
       is_false "$cam_ep" || { echo "FAIL readback camera_epoch"; exit 4; } ;;
