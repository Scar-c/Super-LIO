# 13: [Super-LIVO v0][TB-12] Enable full MODE-C Common-FEJ

**Status:** ready-for-agent

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v0 — Camera-Epoch Sequential LIVO with Common-FEJ）

## Source specification

`docs/super_livo/specs/super_livo_v0_spec.md` → **TB-12 节**、**FEJ Modes 节**（MODE-C）、ADR-004。

## Goal

启用完整 MODE-C Common-FEJ：组合 Common-FEJ LiDAR（TB-11）+ Sequential VIO + Visual H(x_F)：

```text
H_L = H_L(x_F)
H_C = H_C(x_F)
r_C = r_C(x_cur)
```

## Why this tracer bullet exists

ADR-004 最终推荐模式：LIO/VIO 共享同一局部 tangent-space geometry，为 Phase D/F（degeneracy awareness）铺路。

## What to build

- mode `livo_common_fej`：LiDAR final rebuild（TB-11）+ VIO prior=(x_L, P_L)（TB-3）+ 视觉 Jacobian 冻结于 x_F（TB-10 机制）。
- TB-10（H 冻结 checksum）与 TB-11（ΔF / Λ_L eigenvalues）instrumentation 同时输出。
- 合成 oracle（TB-11 已建）作为本 TB 验收前置。

## Explicit non-goals

- **禁止要求 nonlinear MODE-A == joint**（不写这类测试/断言）。
- 无 adaptive noise；无 camera-only；无 exposure state。
- 不改变 ADR（若必须改变 → STOP + ADR REVIEW REQUIRED）。

## Architectural invariants

- I-06/08/09；ADR-004 MODE-C 定义。

## Likely code seams

- `livo/` orchestrator 模式调度、ESKF observation context、visual observation。

## Tests required

- full MODE-A/B/C dataset（公平消融）+ 合成 oracle（TB-11 已建）。

## Acceptance criteria

- [ ] MODE-A/B/C 同输入（相同 sync/VisualMap/点集/patch/outlier gate/iterations/噪声/map lifecycle）均可运行；唯一差异为 linearization policy（脚本对比配置 diff 证明）。
- [ ] 合成 oracle：同一固定 x_F / H_L / H_C / R_L / R_C / P_prior 下，joint linear solve 与 LiDAR→Visual sequential linear solve 数值一致（rel err 声明在测试内）。
- [ ] 测试/代码中不存在断言 nonlinear MODE-A == joint 的条目。
- [ ] MODE-C 运行中 TB-10（H 冻结 checksum）与 TB-11（ΔF / Λ_L eigenvalues）诊断同时输出且有效。
- [ ] 三模式消融结果（轨迹/诊断）分别落盘（results/<experiment>/<mode>/）。

## Runtime / memory / debug artifacts

- fej_stats.csv / visual_stats.csv / lio_stats.csv（三模式均完整）。

## Failure rules

- FAIL：模式切换导致行为突变、消融变量不单一、oracle 不一致。
- 允许改：orchestrator 调度细节。
- 禁止用于救场：给 MODE-C 单独换噪声/kernel 来"好看"。

## Blocked by

TB-11 (`12-tb11.md`)

## Unblocks

TB-13 (`14-tb13.md`)

## Commit policy

1 accepted TB ≈ 1 logical milestone commit：tests/gates PASS → code review → 一个逻辑 commit → push origin/super-livo。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。