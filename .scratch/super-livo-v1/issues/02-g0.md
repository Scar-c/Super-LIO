# 02: [Super-LIVO v1][G-0] Add shadow micro-surfel sufficient statistics

**Status:** completed（Round 8；oracle PASS、eee/nya bitwise parity、evidence docs/super_livo/evidence/g0_micro_surfel_statistics.md）

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v1）

## Source specification

`docs/super_livo/specs/super_livo_v1_spec.md` → **对应 TB 节**；`docs/super_livo/redesign/architecture_owner_decisions.md`。

## Goal

Candidate C sparse sidecar + Welford centered scatter（float storage / double arithmetic / double eig），与 estimator 完全解耦的 shadow。

## Why this tracer bullet exists

证明 0.25m micro-surfel 统计正确性与成本前，不改 OctVox ABI；G-1..G-3 全部依赖 G-0 stats。

## What changes

新增 geometry/ 模块：sidecar（KEY→parent stats block，N≥2 才分配）；Welford 增量更新；同一 accepted point set + 同一 centroid 语义；统计输出。**不接入 Observe/ESKF**。

## What does NOT change

OctVox ABI；HKNN/plane fitting/IESKF；estimator 行为（轨迹必须 bitwise 不变）；NOT production OctVox inline 改造。

## Architectural invariants

I-01（OctVox payload 不变——shadow 用 sidecar）；fixed geometry 0.5/8/0.25/N20/0.1m。

## Likely source seams

新 geometry/ 模块；UpdateMap 后 shadow 挂钩；offline 周期诊断扩展。

## Instrumentation

stats update time/frame；sidecar allocation count；sizeof 实测（baseline OctVox、C 侧边条目、A/B theoretical 104→296B/parent 与 alignment 后实际值分开记录）；RSS。

## Tests required

minimal synthetic oracle：plane / noisy plane / line / non-planar cluster / N=1..20；incremental vs brute-force double：mean/scatter/covariance/normal/eigen ordering/plane-valid classification。无 10km stress 要求。

## Dataset / execution policy

eee_01（offline，publish=false；同 TB-0 实验基础设施 results/<experiment>/<run_id>/）。

## Acceptance criteria

- [ ] oracle：全部 synthetic 用例 incremental 与 brute-force double 一致（误差在测试中声明，float storage 误差记录）。
- [ ] shadow 开启后 eee_01 全量 trajectory MD5 与 baseline 完全一致（无 estimator 影响；与 G-0 前对照）。
- [ ] sizeof 实测完成并分别记录：baseline sizeof(OctVox)、C 侧边条目、A/B theoretical（104→296B/parent）与 alignment 后实际值。
- [ ] runtime/memory 分项输出：stats update、sidecar lookup、RSS。
- [ ] sidecar 分配计数 = N≥2 subvoxel 数（断言可核对）。

## Failure rules

oracle 不一致 / trajectory 变化 / 偷偷改 OctVox production ABI → FAIL；只修 stats 实现；禁止为通过改 estimator。

## Allowed next step

G-1（occupancy 统计）

## Blocked by

TB-1（`01-tb1.md`）

## Unblocks

G-1（`03-g1.md`）

## Commit policy

1 accepted tracer bullet → required tests/gates PASS → review → 一个逻辑 milestone commit → push origin/super-livo → STOP。
DG-0：NO code commit required（owner approval 后按当时指令处理 docs/tracker）。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins / refs/BIEVR-LIO = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。

## Scope prohibitions（除非本 tracer 明确要求）

adaptive R / exposure state / camera-only landmark / stereo triangulation / inverse depth /
loop closure / backend optimization / GPU / deep learning / neighbor micro-surfel search /
voxel resolution sweep —— 一律禁止。
