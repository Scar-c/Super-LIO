# 15: [Super-LIVO v1][L-0] Rebuild final LiDAR observation for Common-FEJ

**Status:** ready-for-agent（frontier 内；依赖完成后可开始）

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v1）

## Source specification

`docs/super_livo/specs/super_livo_v1_spec.md` → **对应 TB 节**；`docs/super_livo/redesign/architecture_owner_decisions.md`。

## Goal

x_F=x_search 后 final rebuild；correspondence source 冻结（MICRO_SURFEL_DIRECT 或 HKNN_FALLBACK）。

## Why this tracer bullet exists

Common-FEJ 核心（ADR-003 Stage C）；兼容 DG-0 选定的 production geometry path。

## What changes

final nonlinear correspondence cache 区分 source type；DIRECT → 复用同一 micro-surfel plane/version（NO HKNN/NO fit）；HKNN_FALLBACK → 复用 final cached HKNN fitted plane（不重 fit）；ΔF 监控。

## What does NOT change

final rebuild 不做新 correspondence selection；无 neighbor；reuse final association（DECISION-07）。

## Architectural invariants

I-08/I-09；source 冻结语义。

## Likely source seams

Observe final cache + ESKF first update + FEJ orchestration。

## Instrumentation

每帧 ‖Δp_F‖/‖Δθ_F‖、Λ_L eigenvalues、ΔF 分布（median/P90/P95/P99/max）；source type 计数。

## Tests required

joint-vs-sequential 合成 oracle + source 冻结断言。

## Dataset / execution policy

eee_01（offline）。

## Acceptance criteria

- [ ] DIRECT 路径 rebuild 中 HKNN/plane fit 调用计数为 0（instrumentation）。
- [ ] HKNN_FALLBACK 路径不重新 fit（复用缓存证明）。
- [ ] final rebuild 不换 plane source（同帧 correspondence source 不变断言）。
- [ ] ΔF 分布输出；不设置 re-anchor threshold（OPEN-01）。
- [ ] 合成 oracle：joint solve == sequential（rel err 测试声明）。

## Failure rules

rebuild 重选 correspondence/重 HKNN（DIRECT 时）→ FAIL；缓存生命周期不足走 ADR amendment。

## Allowed next step

V-6

## Blocked by

V-5（`14-v5.md`）

## Unblocks

V-6（`16-v6.md`）

## Commit policy

1 accepted tracer bullet → required tests/gates PASS → review → 一个逻辑 milestone commit → push origin/super-livo → STOP。
DG-0：NO code commit required（owner approval 后按当时指令处理 docs/tracker）。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins / refs/BIEVR-LIO = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。

## Scope prohibitions（除非本 tracer 明确要求）

adaptive R / exposure state / camera-only landmark / stereo triangulation / inverse depth /
loop closure / backend optimization / GPU / deep learning / neighbor micro-surfel search /
voxel resolution sweep —— 一律禁止。
