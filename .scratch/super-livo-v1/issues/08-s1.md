# 08: [Super-LIVO v1][S-1] Add explicit sequential-prior ESKF API

**Status:** ready-for-agent（frontier 内；依赖完成后可开始）

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v1）

## Source specification

`docs/super_livo/specs/super_livo_v1_spec.md` → **对应 TB 节**；`docs/super_livo/redesign/architecture_owner_decisions.md`。

## Goal

ESKF 支持显式 prior 的 second sequential observation；角色化语义（PropagationPrior / SequentialPrior / LinearizationAnchor）；VIO prior=(x_L,P_L)。

## Why this tracer bullet exists

ADR-003 盒式语义落地；禁止 state_propagat 兼任多语义。

## What changes

UpdateObserve(prior, obs)→PosteriorSnapshot；角色化 snapshot 类型；first observation → x_L,P_L；second observation prior = x_L,P_L。

## What does NOT change

第一 observation 数值行为（parity）；IEKF 迭代语义（prior 固定、P 循环外更新）；无 photometric residual。

## Architectural invariants

I-06（prior≠FEJ）；I-07（VIO prior=(x_L,P_L)）；无 mixed-anchor。

## Likely source seams

lio/ESKF.h/.cpp。

## Instrumentation

无新增（API + unit）。

## Tests required

zero-information second obs（Λ=0,b=0 → x_post=x_L, P_post=P_L，容差测试声明）；first-observation parity。

## Dataset / execution policy

unit + eee_01 parity（offline）。

## Acceptance criteria

- [ ] dummy oracle：Λ=0,b=0 后 x_post==x_L、P_post==P_L（数值容差内）。
- [ ] first observation 与 S-0 行为等价（parity 验证）。
- [ ] 三语义在类型/命名可辨；无共享可变变量兼任 prior 与 FEJ anchor。

## Failure rules

dummy 不通过/mixed-anchor 复辟 → FAIL。

## Allowed next step

V-0

## Blocked by

S-0（`07-s0.md`）

## Unblocks

V-0（`09-v0.md`）

## Commit policy

1 accepted tracer bullet → required tests/gates PASS → review → 一个逻辑 milestone commit → push origin/super-livo → STOP。
DG-0：NO code commit required（owner approval 后按当时指令处理 docs/tracker）。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins / refs/BIEVR-LIO = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。

## Scope prohibitions（除非本 tracer 明确要求）

adaptive R / exposure state / camera-only landmark / stereo triangulation / inverse depth /
loop closure / backend optimization / GPU / deep learning / neighbor micro-surfel search /
voxel resolution sweep —— 一律禁止。
