# 06: [Super-LIVO v1][DG-0] Decide micro-surfel production geometry after feasibility evidence

**Status:** BLOCKED FOR OWNER REVIEW（直到 G-0..G-3 证据齐备且 Architecture Owner 明确批准；NO AUTO-CLOSE / NO AUTO-PROMOTE）

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v1）

## Source specification

`docs/super_livo/specs/super_livo_v1_spec.md` → **DECISION GATE**；`docs/super_livo/redesign/architecture_owner_decisions.md` §6/§7/§14/§15。

## Status note

Implementation: NONE。接受条件不是"代码 PASS"，而是：all required G evidence present + Architecture Owner decision recorded。

## Goal

ARCHITECTURE DECISION GATE：汇总 G-0..G-3 证据，由 Architecture Owner 决定 production storage、plane gate、direct-plane 阈值、sync 阈值与总体 GO/MARGINAL/NO-GO。

## Why this tracer bullet exists

这是 v1 最重要的 architecture hold point；实现者不得自动通过或自动提升（NO AUTO-CLOSE / NO AUTO-PROMOTE）。

## Evidence reviewed


- Geometry correctness: Welford oracle、plane validity behavior、micro-vs-HKNN normal/residual agreement
- Coverage: R3/R5/R8/R10/R20、Rplane_point、Rplane_voxel、Rgrid_plane
- Causality/visibility: mature_while_visible、plane_valid_while_visible、maturity delays
- LiDAR fast-path: falling direct coverage、HKNN fallback、reject、runtime comparison
- Memory: baseline、Candidate C shadow、Candidate A/B modeled/sizeof、RSS
- Runtime: stats update、eig、sidecar lookup、direct shadow、original HKNN/plane fitting


## Decisions required


1. Production geometry storage: A / B / C / revised candidate
2. Provisional plane gate: q_flat、q_line
3. Direct-plane: d_max、d_t,max
4. Geometry sync: normal-angle threshold、anchor/depth threshold
5. Overall verdict: GO / MARGINAL / NO-GO


## What does NOT change

上述 5 项在 DG-0 之前都不是 implementer 可静默决定的最终常数。

## Architectural invariants

fixed geometry；production storage 之前不改 OctVox production ABI；S-0 不得在 owner 关闭 DG-0 前开始。

## Likely source seams

无（DOCUMENTATION + 决策记录）。

## Instrumentation

证据汇总文档（数据表）。

## Tests required

无新增测试（G-0..G-3 证据即输入）。

## Dataset / execution policy

eee_01 / Corridor01 / SFS 证据（按 promotion 顺序）。

## Acceptance criteria

- [ ] 全部 required G evidence 齐备（§13 清单逐项存在）。
- [ ] Architecture Owner decision recorded（storage/plane gate/direct thresholds/sync thresholds/verdict）。
- [ ] Status 仅在 owner 明确批准后置为完成；之前恒为 BLOCKED FOR OWNER REVIEW。
- [ ] NO code commit required。

## Failure rules

G 证据缺失 / 实现者自行选定 storage 或阈值 / 自动放行 S-0 → FAIL。

## Allowed next step

S-0（仅 owner 批准后）

## Blocked by

G-3（`05-g3.md`）

## Unblocks

S-0（`07-s0.md`）—— 仅 owner 批准后

## Commit policy

1 accepted tracer bullet → required tests/gates PASS → review → 一个逻辑 milestone commit → push origin/super-livo → STOP。
DG-0：NO code commit required（owner approval 后按当时指令处理 docs/tracker）。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins / refs/BIEVR-LIO = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。

## Scope prohibitions（除非本 tracer 明确要求）

adaptive R / exposure state / camera-only landmark / stereo triangulation / inverse depth /
loop closure / backend optimization / GPU / deep learning / neighbor micro-surfel search /
voxel resolution sweep —— 一律禁止。
