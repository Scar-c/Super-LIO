# 17: [Super-LIVO v1][M-0] Finalize geometry and visual map ordering and lifetime

**Status:** ready-for-agent（frontier 内；依赖完成后可开始）

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v1）

## Source specification

`docs/super_livo/specs/super_livo_v1_spec.md` → **对应 TB 节**；`docs/super_livo/redesign/architecture_owner_decisions.md`。

## Goal

最终 update ordering（x_LC 插入）+ geometry/visual/sidecar lifetime 联动。

## Why this tracer bullet exists

DECISION-18：防止先以 x_L 插图再被 VIO 改变；ADR-006 AMEND（plane validity 生命周期）。

## What changes

顺序：IMU → LiDAR search → common anchor → final LiDAR posterior → visual update → x_LC → geometry map update → visual map update；eviction → exposes evicted key → orchestrator → VisualMap erase + micro-surfel sidecar stats 清理。

## What does NOT change

地图结构/LRU 语义；visual lifetime ≤ geometry lifetime（无 orphan landmark 独立活过 source geometry）。

## Architectural invariants

I-10；ADR-006；first version 不允许 orphan landmark 存活过 source geometry。

## Likely source seams

orchestrator（UpdateMap 调用点）+ eviction 传播。

## Instrumentation

插入计数、eviction 联动计数。

## Tests required

component（顺序断言）+ automated eviction（小 capacity）+ dataset。

## Dataset / execution policy

eee_01（offline，全模式）。

## Acceptance criteria

- [ ] geometry 插入 pose == x_LC（component 断言：插入时 pose 与最终发布 pose 一致）。
- [ ] 不存在"以 x_L 插入后再修改"路径（顺序断言+日志）。
- [ ] eviction automated test：geometry key evicted → 对应 visual 条目与 sidecar stats 同步移除。
- [ ] 全模式（MODE-A/B/C）dataset 运行 map 无损坏、无 orphan。

## Failure rules

插入早于 VIO final/插入 pose≠x_LC/联动失效 → FAIL。

## Allowed next step

v1 收尾评审

## Blocked by

V-6（`16-v6.md`）

## Unblocks

None（final）

## Commit policy

1 accepted tracer bullet → required tests/gates PASS → review → 一个逻辑 milestone commit → push origin/super-livo → STOP。
DG-0：NO code commit required（owner approval 后按当时指令处理 docs/tracker）。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins / refs/BIEVR-LIO = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。

## Scope prohibitions（除非本 tracer 明确要求）

adaptive R / exposure state / camera-only landmark / stereo triangulation / inverse depth /
loop closure / backend optimization / GPU / deep learning / neighbor micro-surfel search /
voxel resolution sweep —— 一律禁止。
