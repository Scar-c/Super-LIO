# 03: [Super-LIVO v1][G-1] Validate micro-surfel planes and visual support coverage

**Status:** NO-GO / HARD BLOCK（Round 8：eee/nya Rplane_point 全 16 gate <20%；evidence docs/super_livo/evidence/g1_visual_geometry_support.md；awaiting Architecture Owner decision on support-scale fallback F1-F4）

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v1）

## Source specification

`docs/super_livo/specs/super_livo_v1_spec.md` → **对应 TB 节**；`docs/super_livo/redesign/architecture_owner_decisions.md`。

## Goal

eee_01 上输出 point/voxel/grid occupancy 与 plane-valid 比例（全部 online causal，禁止未来信息）。

## Why this tracer bullet exists

回答"0.25m micro-surfel 是否有足够覆盖支撑视觉"——DECISION GATE 的核心证据之一。

## What changes

基于 G-0 stats 计算 plane validity（N≥5 eligible + 一个 conservative eigen gate q_flat/q_line + numeric finite gate）；G_FOV 定义（effective candidates ∧ camera FOV ∧ patch border）；R3/R5/R8/R10/R20、Rplane_point、R5_voxel、Rplane_voxel、Rgrid_plane、image occupied span、quadrant coverage。

## What does NOT change

visual feedback OFF；camera-epoch estimator sync OFF（LiDAR-end 时序）；estimator 数值路径；NO VisualMap；NO VIO。

## Architectural invariants

feasibility spec §1-6（causal 计数；分母=effect_mask_ 语义，super_lio.h:75-80）；fixed geometry。

## Likely source seams

G-0 stats + shadow camera association（最近帧规则，记录 offset 分布）+ 投影。

## Instrumentation

周期诊断扩展（每 100/500 epochs：R5/Rplane/voxel/grid）。

## Tests required

合成投影正确性（已知 3D 点 → 已知 FOV/grid cell）。

## Dataset / execution policy

eee_01（offline）；candidate sweep 允许但不要大规模自动调参系统。

## Acceptance criteria

- [ ] eee_01 输出全部 R 系列 + N histogram（N=1/2/3/4/5-7/8-10/11-19/20）+ Rgrid_plane + occupied span + quadrant coverage。
- [ ] 判据报告：GO（median R5≥60% ∧ Rplane≥50% ∧ Rplane_voxel≥40% ∧ P10 Rplane≥20%）/ MARGINAL / NO-GO；Rgrid_plane 必须报告，不设固定 Rgrid 硬门。
- [ ] causal 验证：不使用最终地图未来信息（实现+测试断言）。
- [ ] plane gate 取值记录（q_flat/q_line 从冻结 sweep 范围中取 conservative candidate）。
- [ ] shadow camera association 时间规则与 bias 文档化。

## Failure rules

分母/投影错误、causal 破坏、gate 取值凭空 → FAIL；禁止为覆盖率降低 N 门槛。

## Allowed next step

G-2

## Blocked by

G-0（`02-g0.md`）

## Unblocks

G-2（`04-g2.md`）

## Commit policy

1 accepted tracer bullet → required tests/gates PASS → review → 一个逻辑 milestone commit → push origin/super-livo → STOP。
DG-0：NO code commit required（owner approval 后按当时指令处理 docs/tracker）。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins / refs/BIEVR-LIO = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。

## Scope prohibitions（除非本 tracer 明确要求）

adaptive R / exposure state / camera-only landmark / stereo triangulation / inverse depth /
loop closure / backend optimization / GPU / deep learning / neighbor micro-surfel search /
voxel resolution sweep —— 一律禁止。
