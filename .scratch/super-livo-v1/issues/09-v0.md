# 09: [Super-LIVO v1][V-0] Bind visual landmarks to micro-surfel plane geometry

**Status:** completed（Round 11：VisualLandmark coordinate-origin + 3-slot uint8 观测 + VisualMap sparse side table；sizeof 112B/432B；eee full：102 landmarks/103 slots/0.06MB bounded；evidence v0_visual_map_memory.md）

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v1）

## Source specification

`docs/super_livo/specs/super_livo_v1_spec.md` → **对应 TB 节**；`docs/super_livo/redesign/architecture_owner_decisions.md`。

## Goal

VisualLandmark ↔ MicroSurfel 绑定（1:N）；metric anchor = reference ray × micro-surfel plane（非 centroid μ）。

## Why this tracer bullet exists

0.25m 只定义 3D local plane support，不定义视觉密度；μ ≠ patch center 是已冻结语义。

## What changes

VisualMap 数据结构 + MicroSurfelId={parent_voxel_key, local_idx} source_id；按值 geometry snapshot（μ/n/anchor/last_sync_*/final flag）；ray-plane intersection gate（finite / positive depth / 非平行 / local support）。

## What does NOT change

1 surfel ≠ 1 patch；NO photometric state update；snapshot 不持有 OctVox 内指针；1:N 密度由 image grid 决定。

## Architectural invariants

DECISION-08/10（side-table、immutable anchor）；feasibility G-1 后依据。

## Likely source seams

visual/ 模块 + G-0 stats。

## Instrumentation

绑定计数、snapshot 内存。

## Tests required

ray-plane 求交单测（含 gate：parallel/negative depth/out-of-support）。

## Dataset / execution policy

eee_01（offline shadow 统计延续）。

## Acceptance criteria

- [ ] 绑定可建可查（1:N 断言：同一 MicroSurfelId 可绑定 0/1/多 landmark）。
- [ ] snapshot 无指向 OctVox 容器内对象的指针（类型断言）。
- [ ] ray-plane gate 全分支测试通过（parallel/negative depth/non-finite/out-of-support 拒绝）。
- [ ] anchor ≠ μ（除非 pixel 恰为 μ 投影，测试断言）。

## Failure rules

指针悬挂/μ 当 anchor/1:1 锁死 → FAIL。

## Allowed next step

V-1

## Blocked by

08-s1.md（Round 11 链；原 blocked-by 历史保留：
S-1（`08-s1.md`））

## Unblocks

V-1（`10-v1.md`）

## Commit policy

1 accepted tracer bullet → required tests/gates PASS → review → 一个逻辑 milestone commit → push origin/super-livo → STOP。
DG-0：NO code commit required（owner approval 后按当时指令处理 docs/tracker）。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins / refs/BIEVR-LIO = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。

## Scope prohibitions（除非本 tracer 明确要求）

adaptive R / exposure state / camera-only landmark / stereo triangulation / inverse depth /
loop closure / backend optimization / GPU / deep learning / neighbor micro-surfel search /
voxel resolution sweep —— 一律禁止。
