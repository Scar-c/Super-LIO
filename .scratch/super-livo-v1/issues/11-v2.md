# 11: [Super-LIVO v1][V-2] Validate photometric residual and analytic Jacobian in shadow mode

**Status:** completed（Round 11：plane-aware DC photometric + analytic 6-DOF Jacobian；FD gate PASS（rel 1.5e-3）；streaming pose-only equations；V-3 state-off parity == C0（MD5 d94fd50）；发现问题：landmark 可见性差（V-4 前记录）；evidence v2_photometric_jacobian.md）

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v1）

## Source specification

`docs/super_livo/specs/super_livo_v1_spec.md` → **对应 TB 节**；`docs/super_livo/redesign/architecture_owner_decisions.md`。

## Goal

r_C/J_C shadow + FD 验证；不喂 ESKF。

## Why this tracer bullet exists

任何视觉状态反馈前必须证明数学正确（v0 TB-7 语义保留）。

## What changes

PhotometricEvaluator：bilinear current-image sampling、plane-aware warp（基于 micro-surfel plane snapshot）、8×8 patch、sampling-stride 首版、右扰动 Jacobian 链；FD harness（central difference、rotation/translation、多点多深度多姿态）。

## What does NOT change

state feedback OFF；无 FEJ/adaptive/exposure state。

## Architectural invariants

v1 spec V-2；FD thresholds median e_rel<1e-4、P95<1e-3（若 spec 修改则以 v1 spec 为准）。

## Likely source seams

visual/PhotometricEvaluator。

## Instrumentation

shadow residual 统计落盘。

## Tests required

photometric Jacobian FD unit。

## Dataset / execution policy

unit（合成）+ eee_01 shadow（offline）。

## Acceptance criteria

- [ ] median relative error < 1e-4；P95 < 1e-3（以 v1 spec 数值为准）。
- [ ] rotation 与 translation 分别验证；多 points/depths/poses。
- [ ] ESKF 状态不变（shadow 无 feedback 断言）。
- [ ] warp 几何来自 geometry snapshot（非 mutable OctVox 指针）。

## Failure rules

FD FAIL 只允许修 projection/residual/J/interpolation；禁止调 noise/kernel/FEJ 救 FD。

## Allowed next step

V-3

## Blocked by

10-v1.md（Round 11 链；原 blocked-by 历史保留：
V-1（`10-v1.md`））

## Unblocks

V-3（`12-v3.md`）

## Commit policy

1 accepted tracer bullet → required tests/gates PASS → review → 一个逻辑 milestone commit → push origin/super-livo → STOP。
DG-0：NO code commit required（owner approval 后按当时指令处理 docs/tracker）。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins / refs/BIEVR-LIO = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。

## Scope prohibitions（除非本 tracer 明确要求）

adaptive R / exposure state / camera-only landmark / stereo triangulation / inverse depth /
loop closure / backend optimization / GPU / deep learning / neighbor micro-surfel search /
voxel resolution sweep —— 一律禁止。
