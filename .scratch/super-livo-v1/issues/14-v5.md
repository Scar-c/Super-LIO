# 14: [Super-LIVO v1][V-5] Enable MODE-B VIO-FEJ

**Status:** ready-for-agent（frontier 内；依赖完成后可开始）

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v1）

## Source specification

`docs/super_livo/specs/super_livo_v1_spec.md` → **对应 TB 节**；`docs/super_livo/redesign/architecture_owner_decisions.md`。

## Goal

x_F=x_L；r_C=current、H_C frozen at x_F；冻结可证明。

## Why this tracer bullet exists

FEJ 消融第二档；v0 TB-10 语义保留。

## What changes

mode=livo_vio_fej：FEJ anchor=x_L；同一 visual update 内 active set 冻结、geometry snapshot 冻结、Jacobian checksum 冻结。

## What does NOT change

residual 保持 current；无 Common-FEJ；无 adaptive R；prior 仍 (x_L,P_L)。

## Architectural invariants

I-06；ADR-004 MODE-B。

## Likely source seams

ESKF observation context（LinearizationAnchor）+ visual observation。

## Instrumentation

FEJ anchor id/version、H checksum 落盘。

## Tests required

FEJ freeze unit（checksum 跨 iteration 不变）+ dataset。

## Dataset / execution policy

eee_01（offline）；与 MODE-A 公平消融。

## Acceptance criteria

- [ ] 同一 camera update 所有 iteration 中 FEJ geometry/Jacobian checksum 不变（automated）。
- [ ] active set 与 geometry snapshot 同一 update 内冻结（id 快照断言）。
- [ ] residual 仍按 current 计算（扰动测试证明未冻结）。
- [ ] 除 linearization policy 外与 MODE-A 共享输入/点集/patch/gate/iteration/noise/map lifecycle。

## Failure rules

checksum 变化/active set 未冻结/prior 回 mixed-anchor → FAIL。

## Allowed next step

L-0

## Blocked by

V-4（`13-v4.md`）

## Unblocks

L-0（`15-l0.md`）

## Commit policy

1 accepted tracer bullet → required tests/gates PASS → review → 一个逻辑 milestone commit → push origin/super-livo → STOP。
DG-0：NO code commit required（owner approval 后按当时指令处理 docs/tracker）。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins / refs/BIEVR-LIO = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。

## Scope prohibitions（除非本 tracer 明确要求）

adaptive R / exposure state / camera-only landmark / stereo triangulation / inverse depth /
loop closure / backend optimization / GPU / deep learning / neighbor micro-surfel search /
voxel resolution sweep —— 一律禁止。
