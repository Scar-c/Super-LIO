# 05: [Super-LIVO v1][G-3] Shadow falling-subvoxel LiDAR direct planes against HKNN

**Status:** completed（Round 9：child/parent vs HKNN 三路 shadow；HIGH COVERAGE / MODERATE AGREEMENT；evidence docs/super_livo/evidence/g3_direct_plane_shadow.md）

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v1）

## Source specification

`docs/super_livo/specs/super_livo_v1_spec.md` → **对应 TB 节**；`docs/super_livo/redesign/architecture_owner_decisions.md`。

## Goal

L0（falling subvoxel direct plane）+ L2（原 HKNN fallback）shadow 统计：coverage、normal/residual agreement、fallback rate、runtime。

## Why this tracer bullet exists

回答"falling subvoxel direct plane 本身有多少有效覆盖"——DG-0 的 LiDAR fast-path 证据；neighbor 已 DEFERRED。

## What changes

Observe 内 shadow 路径：对每个 effective point 先查 falling subvoxel plane（plane_valid ∧ |nᵀ(p−μ)|<d_max ∧ d_t=‖(I−nnᵀ)(p−μ)‖<d_t,max），失败走原 HKNN；原 HKNN 结果保持 authoritative。

## What does NOT change

estimator 数值路径（HKNN/plane fit/residual 原样）；NO 6-neighbor / 26-neighbor / arbitrary neighbor search；NO production direct path 启用；IEKF 迭代不变。

## Architectural invariants

direct gate 形式冻结（G1 valid + G2 eigen + G3 distance + G4 tangential）；最终 threshold defer to DG-0（shadow 只记录分布）；neighbor DEFERRED。

## Likely source seams

Observe 的 HKNN 调用点旁路 shadow 计数；G-0 stats query。

## Instrumentation

falling-direct candidate% / gated-good% / HKNN fallback% / reject%；micro-vs-HKNN normal angle、residual difference、plane offset difference（median/P90/P95/P99/max）；runtime（sidecar lookup/eig/direct shadow/HKNN+fit）。

## Tests required

合成点-平面直接命中/拒绝单测（含 d_t 边界）。

## Dataset / execution policy

eee_01（offline）→（通过后）Corridor01 → SFS；d_max/d_t,max 取值记录（数据驱动）。

## Acceptance criteria

- [ ] eee_01 输出全部 shadow 统计（falling-direct/gated-good/fallback/reject + 差异分布 + runtime）。
- [ ] eee_01 全量 trajectory MD5 与 baseline 完全一致（shadow 无副作用；若 estimator state 改变 → FAIL）。
- [ ] d_max / d_t,max 取值记录（无凭空常数）。
- [ ] 代码中不存在 neighbor stencil（6/26-neighbor）实现。
- [ ] 按 promotion rule 依次执行 Corridor01、SFS（eee 未 PASS 不得跳）。

## Failure rules

shadow 改变 estimator 输出 / 出现 neighbor / 无分布数据 → FAIL；禁止为覆盖率加 neighbor 或放宽 gate。

## Allowed next step

DG-0

## Blocked by

G-2（`04-g2.md`）

## Unblocks

DG-0（`06-dg0.md`）

## Commit policy

1 accepted tracer bullet → required tests/gates PASS → review → 一个逻辑 milestone commit → push origin/super-livo → STOP。
DG-0：NO code commit required（owner approval 后按当时指令处理 docs/tracker）。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins / refs/BIEVR-LIO = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。

## Scope prohibitions（除非本 tracer 明确要求）

adaptive R / exposure state / camera-only landmark / stereo triangulation / inverse depth /
loop closure / backend optimization / GPU / deep learning / neighbor micro-surfel search /
voxel resolution sweep —— 一律禁止。
