# 13: [Super-LIVO v1][V-4] Enable MODE-A sequential visual update

**Status:** ready-for-agent（frontier 内；依赖完成后可开始）

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v1）

## Source specification

`docs/super_livo/specs/super_livo_v1_spec.md` → **对应 TB 节**；`docs/super_livo/redesign/architecture_owner_decisions.md`。

## Goal

首次 camera 修改 estimator state：VIO prior=(x_L,P_L)、residual current、Jacobian current、iterative relinearization。

## Why this tracer bullet exists

干净 baseline（无 FEJ）；v0 TB-9 语义保留。

## What changes

mode=livo_sequential 接入 streaming 观测；correction 诊断输出。

## What does NOT change

NO FEJ；NO Common-FEJ；NO adaptive R；不以 ATE improved 为唯一 correctness gate。

## Architectural invariants

I-07；ADR-004 MODE-A。

## Likely source seams

ESKF second update（S-1）+ orchestrator + visual observation。

## Instrumentation

每 camera update：active/accepted/rejected、‖Δp_visual‖、‖Δθ_visual‖、residual 分位、Λ_C condition/eigenvalues。

## Tests required

dataset（eee_01 offline）+ 诊断；visual OFF 复现性。

## Dataset / execution policy

eee_01（offline）→ Corridor01 → SFS（promotion）。

## Acceptance criteria

- [ ] finite trajectory/covariance、无爆炸修正、residual/iteration bounded。
- [ ] visual OFF（S-0 camera_epoch 模式）复现 pre-V-4 行为（同 parity 规则）。
- [ ] correction 诊断字段全部输出。
- [ ] 无 ATE 断言。

## Failure rules

爆炸性修正/visual OFF 不复现 → FAIL；禁止调噪声/换 kernel 救场。

## Allowed next step

V-5

## Blocked by

12-v3.md（Round 11 链；原 blocked-by 历史保留：
V-3（`12-v3.md`））

## Unblocks

V-5（`14-v5.md`）

## Commit policy

1 accepted tracer bullet → required tests/gates PASS → review → 一个逻辑 milestone commit → push origin/super-livo → STOP。
DG-0：NO code commit required（owner approval 后按当时指令处理 docs/tracker）。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins / refs/BIEVR-LIO = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。

## Scope prohibitions（除非本 tracer 明确要求）

adaptive R / exposure state / camera-only landmark / stereo triangulation / inverse depth /
loop closure / backend optimization / GPU / deep learning / neighbor micro-surfel search /
voxel resolution sweep —— 一律禁止。
