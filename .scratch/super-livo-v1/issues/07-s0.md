# 07: [Super-LIVO v1][S-0] Add camera-epoch synchronization with visual feedback off

**Status:** COMPLETED（Round 11 corrective：SIGSEGV double-pop 修复 + FAST-LIVO2 语义重构；10s/30s/full eee/nya PASS；legacy bitwise parity；conservation tests PASS；evidence s0_sync_crash_fastlivo2_corrective.md + s0_fast_livo2_sync_reference.md）

## Parent

本地 tracker parent：`00-parent.md`（Super-LIVO v1）

## Source specification

`docs/super_livo/specs/super_livo_v1_spec.md` → **对应 TB 节**；`docs/super_livo/redesign/architecture_owner_decisions.md`。

## Goal

camera-epoch 同步（visual OFF）：t_c 切分 LiDAR、IMU 传播到 t_c、deskew 到 t_c、LIO at t_c；仅 DG-0 approved 后可执行。

## Why this tracer bullet exists

ADR-001 时间架构落地；原 v0 TB-2 语义保留。

## What changes

/sync/mode=camera_epoch；唯一边界规则 t≤tc→current / t>tc→future；legacy lidar_end 路径保留；每 epoch debug 字段（tc/interval/点计数/IMU interval/deskew ref）。

## What does NOT change

LIO 内部算法；NO VIO / NO photometric / NO VisualMap；不要求 camera-epoch 轨迹与 legacy bitwise identical（P2 sanity 规则）。

## Architectural invariants

conservation（无丢/重/边界双消费）；P1 legacy parity；P2 camera-epoch sanity；bounded buffers；offline equivalent path。

## Likely source seams

sync_measure/ROSWrapper + Propagation_Undistort（deskew 目标时刻 t_c）；offline backend。

## Instrumentation

每 epoch 切分 debug + accounting。

## Tests required

conservation unit+integration；P1/P2 dataset。

## Dataset / execution policy

eee_01（offline 优先；online 仅 parity）。

## Acceptance criteria

- [ ] consumed+carried == input（unit+integration 断言，无重复/丢失/边界双消费）。
- [ ] 边界点 t==t_c 恰好消费一次（规则唯一）。
- [ ] lidar_end 模式保持 TB-0 级 parity（MD5 或最大 delta 报告）。
- [ ] camera_epoch 模式：timestamps 单调、state/cov finite、buffer 有界、无数量级恶化（不以 ATE 提升为门）。
- [ ] offline 与 online 等价路径 parity 报告。

## Failure rules

守恒违反/parity 破坏/buffer 无界 → FAIL；禁止把 scan-end 冒充 camera-epoch。

## Allowed next step

S-1

## Blocked by

20-g1vr-coordinate-origin-reparameterization.md（Round 11 链；原 blocked-by 历史保留：
DG-0 Architecture Owner approval（`06-dg0.md`））

## Unblocks

S-1（`08-s1.md`）

## Commit policy

1 accepted tracer bullet → required tests/gates PASS → review → 一个逻辑 milestone commit → push origin/super-livo → STOP。
DG-0：NO code commit required（owner approval 后按当时指令处理 docs/tracker）。

## Reference repo rule

refs/FAST-LIVO2 / refs/open_vins / refs/BIEVR-LIO = READ ONLY（允许搜索/阅读/diff，禁止修改/commit/patch）。

## Scope prohibitions（除非本 tracer 明确要求）

adaptive R / exposure state / camera-only landmark / stereo triangulation / inverse depth /
loop closure / backend optimization / GPU / deep learning / neighbor micro-surfel search /
voxel resolution sweep —— 一律禁止。
