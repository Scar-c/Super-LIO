# Super-LIO × Oxford

`NOT_PUBLISHED`: no Oxford/Oxford-Spires dataset config or launch was found in
the method-author `ros1` tree or its reachable config history at `60b57aa`.
The Super-LIVO-derived Oxford config is not relabeled as pristine Super-LIO.
