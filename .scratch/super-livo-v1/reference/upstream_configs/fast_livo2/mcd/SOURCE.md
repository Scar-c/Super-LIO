# FAST-LIVO2 × MCD

`FAST_LIVO2_MCD_DATASET_CONFIG = NOT_FOUND` after auditing the method-author
tree and primary GitHub search. The stored Avia/pinhole files are method-author
defaults only. They are a Visual seed reference, not an official MCD config and
not directly runnable on MCD without explicit sensor/topic/calibration adaptation.
