# FAST-LIVO2 × M3DGR

Dataset-author adapted implementation from `sjtuyinjie/M3DGR@e0cf7d5`. For
Corridor01 the Avia config is selected; it loads the M3DGR pinhole camera file,
uses camera offset `+0.1 s`, and is evaluated by the ArUco relative-transform
path established in Round11AB. Pristine reproduction pending.
